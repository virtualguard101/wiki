---
date: 2026-04-05 21:25:09
title: 关系数据库理论
permalink: relational-db-theory
publish: true
tags:
  - 数据库系统
---

# 关系数据库理论

## 规范化解决的问题

把很多信息塞进一张大表，容易出现（以教学管理为例）：

1. **数据冗余**：系名、系主任等随选课重复存储。

2. **插入异常**：主码含学号时，没学生的新课可能无法插入。

3. **删除异常**：删光某系学生时，系信息也跟着没了。

4. **更新异常**：换系主任要改很多行，易不一致。

**思路**：通过 **[关系模式的分解](关系模式的分解.md)**，使每个关系描述尽量单一的概念或联系，并满足一定范式要求。

---

## 函数依赖

### 语义

- 若属性组 $X$ 的值能**唯一决定** $Y$ 的值，则称 **$Y$ 函数依赖于 $X$**，记作 $X \to Y$。  

!!! tip
    依赖是对**模式上一切合法实例**都要成立的约束，不是「看当前这张表凑出来的」；是否成立由业务规则决定（例如是否允许重名会决定「姓名→年龄」是否成立）。

### 常用术语

!!! tip
    非平凡依赖一般指 $Y$ 不是 $X$ 的子集。

- **决定因子**：$X \to Y$ 中的 $X$。

- **完全 / 部分依赖**：若 $X \to Y$，且 $X$ 的**任一真子集**都不能决定 $Y$（真子集 $X' \not\to Y$），则 $Y$ **完全**依赖于 $X$；若存在 $X$ 的**某一真子集** $X'$ 使 $X' \to Y$，则 $Y$ **部分**依赖于 $X$。（分析 2NF 时常看**非主属性**对**候选码**是完全还是部分依赖。）

- **传递依赖**：$X \to Y$，$Y \to Z$，且 $Y$ 不决定 $X$、$Z$ 不⊆$Y$ 等条件满足时，$Z$ **传递**依赖于 $X$。

- **候选码**：属性（组）$K$ 若满足 $K \to U$（决定全部属性），且 $K$ 的任一真子集都不能决定 $U$，则 $K$ 为**候选码**；任选其一可作**主码**。出现在任一候选码中的属性为**主属性**，否则为**非主属性**。  
若 $X$ 不是本关系的码，但是别的关系的码，则 $X$ 是本关系的**外码**，用来表示表间联系。

    !!! tip "求候选码的快捷想法"
        把属性按在 $F$ 中只出现在左、只出现在右、两边都出现、都不出现分类（**L / R / LR / N**）；若由 **L 与 N** 组成的集合的闭包已是全部属性，则它常是**唯一候选码**（充分条件）。更复杂情形见教材算法。

### 逻辑蕴含

已知 $F$ 中有 $A \to B$、$B \to C$，则 $A \to C$ 也被 $F$ **蕴含**。为判断与求码，常用 **Armstrong 公理**（自反、增广、传递）及推出的合并、伪传递、分解等规则；**属性集闭包** $X^+$：从 $X$ 出发反复用 $F$ 扩张，直到不能再加属性为止，用于判断 $X \to Y$ 是否被 $F$ 蕴含、求候选码等。

### 最小依赖集

右部单属性、无冗余依赖、左部无多余属性；与 $F$ 等价但更精简，便于分解与理解。

---

## 范式（1NF ~ BCNF，提要）

- **1NF**：每个属性都是**不可再分**的基本项（表中无「表」、无多值格直接塞一格）。

- **2NF**：在 1NF 基础上，每个**非主属性**对**任一候选码**都是**完全函数依赖**（不能只靠码的一部分）。

- **3NF**：在 2NF 基础上，**非主属性**不**传递依赖**于任一候选码。

- **BCNF**：更严——任一**非平凡** $X \to Y$ 中，决定因子 $X$ 都必须**包含某个候选码**（通俗说：「谁决定谁」里的决定方都应是码级）。  
满足 BCNF 则必满足 3NF；反之不一定。

!!! example
    「学生、系、系主任、课号、成绩」若码为 (学号, 课号)，则系、系主任**部分**依赖于码 → 最高 **1NF**，拆成选课(学号,课号,成绩)与学生-系等可往 2NF、3NF 走。

- **STJ(S,T,J)**，语义下可有 $T \to J$ 而 $T$ 不含码 → **3NF 可满足但非 BCNF**（课件例）。

- **4NF**（了解）：消除**非平凡多值依赖**中决定方不是超码的情况；与「一对多」独立信息叠在一张表里产生的冗余有关（如医生–患者–电话例）。

- 范式层级（了解包含关系即可）：$\text{5NF} \subseteq \text{4NF} \subseteq \text{BCNF} \subseteq \text{3NF} \subseteq \text{2NF} \subseteq \text{1NF}$。

---

## 设计时记三句话

1. **一事一地**：尽量让一个关系只表达一个实体或一种联系。  

2. **分解不是越碎越好**：要兼顾 **[无损连接](关系模式的分解.md)** 与 **[保持函数依赖](关系模式的分解.md)**，并权衡查询是否要频繁多表连接。  

3. **实务上**：能到 **BCNF** 最好；若到 BCNF 会**丢失**某些依赖，有时退而取 **3NF** 以保持依赖与无损。

---
